<h1>Listing boilers</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boilers.each do |boiler| %>
  <tr>
    <td><%= boiler.title %></td>
    <td><%= link_to 'Show', [:admin, boiler] %></td>
    <td><%= link_to 'Edit', edit_admin_boiler_path(boiler) %></td>
    <td><%= link_to 'Destroy', [:admin, boiler], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Boiler', new_admin_boiler_path %>
